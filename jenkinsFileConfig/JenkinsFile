pipeline{
    agent any
    triggers{
        cron('')
    }
    stages{
        stage('Tests'){
            steps{
                script {
                    sh 'mvn clean test -Dtest=RunCucumberTest -Dbrowser=Chrome -Dcucumber.options="--tags @CadastroUsuario"'
                }
            }
        }
    }
}
